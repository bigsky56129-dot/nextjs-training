{
  "name": "next-abstract",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "dev": "next dev",
    "build": "next build",
    "start": "next start",
    "export": "next build && next export",
    "test:paths": "node ./scripts/check-base-paths.mjs",
    "deploy": "npm run export && gh-pages -d out -b gh-pages --dotfiles",
    "deploy:project": "npm run test:paths && cross-env NEXT_PUBLIC_BASE_PATH=/nextjs-training npm run export && gh-pages -d out -b gh-pages --dotfiles"
  },
  "dependencies": {
    "next": "13.5.6",
    "react": "18.3.1",
    "react-dom": "18.3.1"
  },
  "devDependencies": {
    "gh-pages": "^6.0.0",
    "cross-env": "^7.0.3"
  }
}
